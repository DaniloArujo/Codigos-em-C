#include <stdio.h>
#include<stdlib.h>

int main(){
	
	int contador;
	int numero = 1;
	
	for(contador = 1; contador <=10; contador = contador +1){
		printf("teste\n");
		printf("asdf\n");		
	}
	system("pause");
	return 0;
}
#include <stdio.h>
#include <stdlib.h>

// Este programa utiliza um loop for para imprimir duas mensagens ("teste" e "asdf") 10 vezes.

int main() {
    // Declarando variáveis
    int contador;
    int numero = 1;
    
    // Utilizando um loop for para repetir a ação 10 vezes
    for (contador = 1; contador <= 10; contador = contador + 1) {
        printf("teste\n");
        printf("asdf\n");
    }
    
    // Pausando a execução
    system("pause");
    return 0;
}
